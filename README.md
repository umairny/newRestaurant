# Tastebud 🍽️

"WHERE EVERY DISH TELLS A STORY"

A modern, elegant, and responsive restaurant website built with React and Vite. Tastebud offers a premium digital experience for customers to explore the menu, make reservations, and read reviews.

## ✨ Features

- **Beautiful UI/UX**: Designed with a "Glassmorphism" aesthetic, featuring smooth animations, elegant typography (Playfair Display & Inter), and a sophisticated dark theme with gold accents.
- **Responsive Design**: Fully responsive layout that looks great on desktop, tablet, and mobile devices.
- **Dynamic Menu**: Browse our full menu with featured items, detailed descriptions, and prices.
- **Reservations**: Easy-to-use reservation system for booking tables.
- **Customer Reviews**: Read what others are saying and see ratings for our dishes.
- **Contact & Location**: responsive contact page with location details.
- **About Us**: Learn about our story and culinary philosophy.

## 🛠️ Tech Stack

- **Framework**: [React](https://react.dev/)
- **Build Tool**: [Vite](https://vitejs.dev/)
- **Styling**: Vanilla CSS (CSS Variables, Flexbox/Grid, Animations)
- **Routing**: [React Router](https://reactrouter.com/)
- **Icons**: [Lucide React](https://lucide.dev/)

## 🚀 Getting Started

Follow these steps to set up the project locally on your machine.

### Prerequisites

- Node.js (v14 or higher)
- npm or yarn

### Installation

1. Clone the repository:
   ```bash
   git clone <repository-url>
   ```

2. Navigate to the project directory:
   ```bash
   cd NewRestaurant
   ```

3. Install dependencies:
   ```bash
   npm install
   ```

4. Start the development server:
   ```bash
   npm run dev
   ```

5. Open your browser and visit `http://localhost:5173` (or the URL shown in the terminal).

## 📂 Project Structure

```
src/
├── assets/         # Static assets like images
├── components/     # Reusable UI components
├── context/        # React Context for state management
├── data/           # Mock data for menu, reviews, etc.
├── pages/          # Application pages (Home, Menu, Contact, etc.)
├── App.jsx         # Main application component
├── main.jsx        # Entry point
└── index.css       # Global styles and variables
```

## 🎨 Design System

The project uses a custom design system defined in `index.css`, utilizing CSS variables for consistent theming:

- **Colors**: Dark mode theme with Gold (`#d4af37`) accents.
- **Typography**: `Playfair Display` for headings and `Inter` for body text.
- **Components**: Glass-morphism cards, styled buttons, and custom inputs.

---

Built with ❤️ for generic restaurant.
